{# @var craft \craft\web\twig\variables\CraftVariable #}
{# @var entry \craft\elements\Entry #}
{# ======================================================
  @filename: error
  @date:     288 Oct 2019
  @author:   sam
  ====================================================== #}
{#
  Template file for when there is an error on the site
#}

{# {% import "_partials/_macros.twig" as macro %} #}
{# uncomment line above if you need to use macros in your template #}
{% set entry = craft.entries.section("genericError").limit(1).one() %}

{% extends "_layouts/_layout.twig" %}

{% block _inline_css %}
  <style>
    {{ source ("error_critical.min.css", ignore_missing = true) }}
  </style>
{% endblock %}

{% block content %}

  <section>
    <div>
      <p>
        {{ 'Generic Error'|t }}
      </p>
    </div>
  </section>

{% endblock %}

{% block _inline_js %}
{% endblock %}
